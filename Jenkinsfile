pipeline {
   agent any
stages {
   stage('dockerhub login'){
        steps{
                sh 'docker login -u sayan556 -p Sayanm15@'
             }
   }
   stage('dockerhub login'){
        steps{
                sh 'docker images'
             }
        }
   
      }
}
